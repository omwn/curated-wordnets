[project]
name = "curated-wordnets"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "tomli; python_version < '3.11'",
    "wn>=1.1",
]

[dependency-groups]
dev = ["pytest"]
